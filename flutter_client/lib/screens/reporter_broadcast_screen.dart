import 'package:flutter/material.dart';

class ReporterBroadcastScreen extends StatelessWidget {
  const ReporterBroadcastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Reporter Broadcast Screen'),
      ),
    );
  }
}
