services:
  - type: web
    name: nexus-play-api
    env: node
    region: oregon
    buildCommand: cd api && npm install
    startCommand: cd api && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
